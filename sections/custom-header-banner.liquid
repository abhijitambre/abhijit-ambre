{%- style -%}
.custom-section {
  font-family: Arial, sans-serif;
}
.header-section {
    display: grid;
    align-items: center;
    justify-content: space-between;
    padding: 10px 20px;
    position: relative;
    grid-template-columns: 1fr 2fr 1fr;
  }

  /* Hamburger Menu */
  .hamburger-menu {
    display: none;
  }

  .hamburger-btn {
    background: none;
    border: none;
    cursor: pointer;
    display: flex;
    flex-direction: column;
    gap: 5px;
  }

  .hamburger-btn span {
    display: block;
    width: 25px;
    height: 3px;
    background: #333;
    transition: transform 0.3s, opacity 0.3s;
  }

  .hamburger-btn.open span:nth-child(1) {
    transform: translateY(8px) rotate(45deg);
  }

  .hamburger-btn.open span:nth-child(2) {
    opacity: 0;
  }

  .hamburger-btn.open span:nth-child(3) {
    transform: translateY(-8px) rotate(-45deg);
  }

  /* Mobile Menu */
  .menu {
    display: none;
  }

  .mobile-menu {
    display: none;
    flex-direction: column;
    position: absolute;
    top: 60px;
    left: 0;
    right: 0;
    background: #fff;
    padding: 20px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    z-index: 9;
  }

  .mobile-menu.menu-open {
    display: flex;
  }

  /* Desktop Text Block */
  .desktop-text {
    display: none;
    font-size: 16px;
    color: #333;
    text-align: center;
    flex-grow: 1;
  }
.logo{
  text-align:center;
}
.header-section .logo img {
  max-height: 50px;
}
.header-section .menu ul {
  display: flex;
  list-style: none;
  margin: 0;
  padding: 0;
}
.header-section .menu ul li {
  margin: 0 10px;
}
  .header-button{
    text-align:right;
  }
.header-button button {
 width: max-content;
  padding:10px 30px;
 height: 3em;
font-size: 15px;
 font-family: inherit;
 border: none;
 position: relative;
 overflow: hidden;
 z-index: 1;
 box-shadow: 6px 6px 12px #c5c5c5,
             -6px -6px 12px #ffffff;
             background:yellow;
  cursor:pointer; 
}

.header-button button::before {
 content: '';
 width: 0;
 height: 3em;
 position: absolute;
 top: 0;
 left: 0;
 background-image: linear-gradient(to right, #111 0%, #111 100%);
 transition: .5s ease;
 display: block;
 z-index: -1;
}
.header-button button:hover{
color:#fff;
}
.header-button button:hover::before {
 width: 100%;
}
.banner-section {
  position: relative;
}
.banner-image {
  width: 100%;
  height: auto;
}
.banner-content {
  position: absolute;
  color: #fff;
  text-align: center;
}
  .banner-content{
    width:40%;
    color:#111;
  }
.banner-content.top-left {
  top: 10%;
  left: 0%;
}
.banner-content.top-right {
  top: 10%;
  right: 0%;
}
.banner-content.center-center {
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}
.banner-content.bottom-left {
  bottom: 5%;
  left: 0%;
}
.banner-content.bottom-right {
  bottom: 5%;
  right: 0%;
}
   .banner-button button {
 width: max-content;
    padding:10px 30px;
 height: 3em;
font-size: 15px;
 font-family: inherit;
 border: none;
 position: relative;
 overflow: hidden;
 z-index: 1;
 box-shadow: 6px 6px 12px #c5c5c5,
             -6px -6px 12px #ffffff;
             background:yellow;
  cursor:pointer; 
}

.banner-button button::before {
 content: '';
 width: 0;
 height: 3em;
 position: absolute;
 top: 0;
 left: 0;
 background-image: linear-gradient(to right, #111 0%, #111 100%);
 transition: .5s ease;
 display: block;
 z-index: -1;
}
.banner-button button:hover{
color:#fff;
} 
.banner-button button:hover::before {
 width: 100%;
}
.promo-carousel {
  text-align: center;
  overflow: hidden;
}
.carousel-wrapper {
  display: flex;
  animation: scroll-text 10s linear infinite;
}
.carousel-slide {
  min-width: 100%;
  font-size: 18px;
  font-weight: bold;
  color: #111;
}
  
.carousel-slide p{
  padding:0;
  margin:0;
  font-size:16px;
}


 
@keyframes scroll-text {
  0% {
    transform: translateX(0%);
  }
  100% {
    transform: translateX(-100%);
  }
}
  .md-block{
    display:none;
  }
  
  @media (max-width: 768px) {
    .menu {
      display: none;
    }

    .hamburger-menu {
      display: block;
    }

    .desktop-text {
      display: none;
    }
    .md-block{
    display:block;
  }
    .md-none{
    display:none;
  }
    .banner-content1{
      position-relative
    }
    .header-button button, .banner-button button{
 width: max-content;
  padding:6px 15px;
 height: 3em;
font-size: 12px;
}
    .carousel-slide p {
    padding: 0;
    margin: 0;
    font-size: 10px;
}
    .product-box .floating-btn{
      width:14px;
    }
  }
  
  @media (min-width: 769px) {
    .hamburger-menu {
      display: none;
    }

    .desktop-text {
      display: block;
    }
  }
  
{%- endstyle -%}


<div class="custom-section">
  <!-- Header Section -->
  <header class="header-section">
    <!-- Hamburger Menu (Visible only in Mobile View) -->
    <div class="hamburger-menu">
      <button class="hamburger-btn" aria-label="Toggle Menu">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>

    <!-- Logo -->
    <div class="logo">
      <a href="{{ shop.url }}">
        <img src="{{ section.settings.logo_img | img_url: 'master' }}" alt="{{ shop.name }}" />
      </a>
    </div>

    <!-- Desktop Text Block (Visible only in Desktop View) -->
    <div class="desktop-text">
      <p>{{ section.settings.desktop_text }}</p>
    </div>

    <!-- Header Button -->
    <div class="header-button">
      <a href="{{ section.settings.header_button_url }}" class="btn sliding-btn"><button>{{ section.settings.header_button_text }} &rarr;</button></a>
    </div>

    <!-- Mobile Menu -->
    <nav class="menu mobile-menu">
      <ul>
        {% for link in linklists.main-menu.links %}
        <li><a href="{{ link.url }}">{{ link.title }}</a></li>
        {% endfor %}
      </ul>
    </nav>
  </header>

  <!-- Banner Section -->
  <div class="banner-section">
    <div class="banner-content1 md-block {{ section.settings.content_position }}">
      <h1>{{ section.settings.banner_heading }}</h1>
      <p>{{ section.settings.banner_description }}</p>
    </div>
    {% if section.settings.banner_image %}
    <img src="{{ section.settings.banner_image | img_url: 'master' }}" alt="Banner" class="banner-image" />
    {% endif %}
    <div class="banner-content {{ section.settings.content_position }}">
      <h1 class="md-none">{{ section.settings.banner_heading }}</h1>
      <p class="md-none">{{ section.settings.banner_description }}</p>
      {% if section.settings.banner_button_url and section.settings.banner_button_text %}
      <a href="{{ section.settings.banner_button_url }}" class="btn banner-button"><button >{{ section.settings.banner_button_text }} &rarr;</button></a>
      {% endif %}
    </div>
  </div>

  <!-- Promotional Text Carousel -->
  <div class="promo-carousel">
    <div class="carousel-wrapper">
      {% for block in section.blocks %}
      <div class="carousel-slide">
        <p>{{ block.settings.promo_text }}</p>
      </div>
      {% endfor %}
    </div>
  </div>
</div>






<!-- Scripts -->
<script>
document.addEventListener('DOMContentLoaded', () => {
  const carouselWrapper = document.querySelector('.carousel-wrapper');
  if (!carouselWrapper) return;

  // Adjust scrolling animation if hovering over text
  carouselWrapper.addEventListener('mouseover', () => {
    carouselWrapper.style.animationPlayState = 'paused';
  });
  carouselWrapper.addEventListener('mouseout', () => {
    carouselWrapper.style.animationPlayState = 'running';
  });
});
  document.addEventListener("DOMContentLoaded", function () {
    const hamburgerBtn = document.querySelector(".hamburger-btn");
    const mobileMenu = document.querySelector(".mobile-menu");

    hamburgerBtn.addEventListener("click", function () {
      mobileMenu.classList.toggle("menu-open");
      hamburgerBtn.classList.toggle("open");
    });
  });
</script>


{% schema %}
{
  "name": "Banner with Header",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo_img",
      "label": "Logo Image"
    },
    {
  "type": "text",
  "id": "desktop_text",
  "label": "Desktop Text",
  "default": "Welcome to our store!"
},

    {
      "type": "image_picker",
      "id": "banner_image",
      "label": "Banner Image"
    },
    {
      "type": "text",
      "id": "banner_heading",
      "label": "Banner Heading"
    },
    {
      "type": "textarea",
      "id": "banner_description",
      "label": "Banner Description"
    },
    {
      "type": "url",
      "id": "header_button_url",
      "label": "Header Button Link"
    },
    {
      "type": "text",
      "id": "header_button_text",
      "label": "Header Button Text"
    },
    {
      "type": "url",
      "id": "banner_button_url",
      "label": "Banner Button Link"
    },
    {
      "type": "text",
      "id": "banner_button_text",
      "label": "Banner Button Text"
    },
    {
      "type": "select",
      "id": "content_position",
      "label": "Banner Content Position",
      "options": [
        { "value": "top-left", "label": "Top Left" },
        { "value": "top-right", "label": "Top Right" },
        { "value": "center-center", "label": "Center Center" },
        { "value": "bottom-left", "label": "Bottom Left" },
        { "value": "bottom-right", "label": "Bottom Right" }
      ],
      "default": "center-center"
    }
  ],
  "blocks": [
    {
      "type": "text_slide",
      "name": "Text Slide",
      "settings": [
        {
          "type": "text",
          "id": "promo_text",
          "label": "Text Content"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Banner with Text Carousel",
      "category": "Custom"
    }
  ]
}
{% endschema %}